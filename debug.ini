FUNC void Coords(void) {
  int idx;

  exec("log > MyValues.log");
  for (idx = 0; idx < 100; idx++) {
    printf ("coordinates[%d] = %d\n", idx, coordinates[idx]);
  }
  exec("log off");
}

FUNC void yaml(void) {
	exec("log > DebugLogs/Params.yaml");
	printf("dest: %d\n", coordToSet);
	printf("final: %d\n", coord);
	printf("pulsesStopIndex: %d\n", stopInd);
	exec("log off");
}

FUNC void c(void) {
  int idx;
	yaml();
	
  exec("log > DebugLogs/TimeCoords.log");
  for (idx = 0; idx < 200; idx++) {
    printf ("%d\t%d\n", times[idx], coordinates[idx]);
  }
  exec("log off");
}